name: Deploy Labs Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN_WORKERS_PROD }}
  DOPPLER_PROJECT: ${{ secrets.DOPPLER_PROJECT_NAME }}
  DOPPLER_CONFIG: ${{ secrets.DOPPLER_CONFIG_NAME }}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Doppler CLI
        run: curl -Ls https://cli.doppler.com/install.sh | sudo sh

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build

      - name: Sync secrets to Cloudflare
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          doppler secrets download --no-file --format env --project "${DOPPLER_PROJECT:-labs}" --config "${DOPPLER_CONFIG:-production}" \
            | npx wrangler pages secret bulk labs

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist --project-name labs --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
